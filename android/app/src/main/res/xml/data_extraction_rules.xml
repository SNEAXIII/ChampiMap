<?xml version="1.0" encoding="utf-8"?>
<!-- allowBackup="false" coupe la sauvegarde cloud, mais pas le transfert d'appareil à appareil (Android 12+,
     targetSdk ≥ 31, indépendant d'allowBackup) : c'est l'exclusion <device-transfer> ci-dessous, pas
     allowBackup, qui protège réellement chunks.db (jusqu'à 500 Mo+) dans ce cas. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
    </device-transfer>
</data-extraction-rules>
